{
  "name": "outageiq-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "if [ -f ../venv/bin/python3 ]; then ../venv/bin/python3 -m unittest discover -s ../tests; else python3 -m unittest discover -s ../tests; fi"
  }
}
